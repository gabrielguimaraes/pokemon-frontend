services:
  main_app:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - ../:/app
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - REACT_APP_API_URL=http://localhost:3001
    command: sh -c "npm install && npm start"
